import { Alert } from "@mui/material";

/** @deprecated Configurações agora são acessadas no contexto de um plano. */
export function ConfigurationListPage() {
  return <Alert severity="info">A configuração agora está disponível em Planos de Verificação.</Alert>;
}
