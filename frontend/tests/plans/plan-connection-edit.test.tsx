import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { expect, it, vi } from "vitest";
import { ConfigurationEditor } from "../../src/configurations/ConfigurationEditor";
import * as api from "../../src/configurations/api";
vi.mock("../../src/configurations/api", async () => ({ ...(await vi.importActual<typeof import("../../src/configurations/api")>("../../src/configurations/api")), getConfiguration: vi.fn().mockRejectedValue({ message: "offline" }), updateConnection: vi.fn(), validateConnection: vi.fn().mockResolvedValue({ validated: true }), getGitLabTree: vi.fn().mockResolvedValue({ items: [] }) }));
it("invalidates then validates changed connection without persisted plaintext", async () => { render(<ConfigurationEditor configuration={{ id: "cfg", name: "Synthetic", gitlab_base_url: "https://gitlab.example.com", enabled: true, target_branch: "main", credential_status: "active", connection_validated: true }} onSaved={vi.fn()} onRun={vi.fn()} />); fireEvent.change(screen.getByLabelText("URL GitLab"), { target: { value: "https://new.example.com" } }); expect(screen.getByRole("button", { name: "Salvar configuração" })).toBeDisabled(); fireEvent.click(screen.getByRole("button", { name: "Validar conexão" })); await waitFor(() => expect(api.updateConnection).toHaveBeenCalled()); expect(screen.getAllByLabelText("Nova credencial")[0]).toHaveValue(""); });
